# Add an entry, where the key represents a slug.
# Provide at least a `name` value and optionally `image`, `description`, etc.
# Markdown is enabled for `description`, however don't use more than one paragraph (enforced by `>`)

first-tag:
  name: First Tag
  title: First Tag
  description: "This is a description for the first tag."

second-tag:
  name: Second Tag
  title: Second Tag
  description: "This is a description for the second tag."
